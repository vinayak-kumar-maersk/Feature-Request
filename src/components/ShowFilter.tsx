import React from 'react';
import { ViewFilter } from '@styles/Event.styled';
const ShowFilter = () => {
  return (
    <div>
    <ViewFilter>
      <a>View filter</a>
    </ViewFilter>
    </div>
  );
}

export default ShowFilter;
